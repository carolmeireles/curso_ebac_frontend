const multiplicar = (num1: number, num2: number) => num1 * num2;

console.log(multiplicar(3, 9));

function saudar(nome: string): string {
    return `Olá, ${nome}!`;
}

console.log(saudar('Carol'));